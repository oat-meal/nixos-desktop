result
*.swp
*.swm
# secrets/network.nix is tracked but encrypted via git-crypt
*.iso

# Experimental packages - not tracked in git
modules/experimental-packages.nix

# Lab CONTENT — kept private/on-prem, never pushed to this public infra repo.
# (See CLAUDE.md "What belongs in this repo". A pre-commit hook also guards these.)
ai-lab/sillytavern/characters/
ai-lab/comfyui/workflows/
# Content file types, wherever they land
*.png
*.jpg
*.jpeg
*.webp
*.gguf
*.safetensors
*.ckpt
*.pt
*.pth
*.onnx
