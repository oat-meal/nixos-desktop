result
*.swp
*.swm
# secrets/network.nix is tracked but encrypted via git-crypt
*.iso

# Experimental packages - not tracked in git
modules/experimental-packages.nix

# SillyTavern character cards - kept private (on-prem only, never pushed to public repo)
ai-lab/sillytavern/characters/
